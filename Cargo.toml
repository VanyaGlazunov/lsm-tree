[package]
name = "lsm-tree"
version = "0.1.0"
edition = "2021"

[dependencies]
tempfile = "3.2"
chunkfs = { git = "https://github.com/Piletskii-Oleg/chunkfs.git", branch = "main", features = ["chunkers", "hashers"] }
criterion = "0.5"
approx = "0.5"

[[bench]]
name = "write_read"
harness = false